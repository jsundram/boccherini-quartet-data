# Generated engraving output (PDFs, MIDI) — rebuilt with `make`
build/

# Zip backups / archives of edited sources
*.zip

# Content-change tooling scratch (auto-cleaned, but ignore if interrupted)
.preview-work-*/
changes/*/.pr-body.md
