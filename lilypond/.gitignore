# Generated engraving output (PDFs, MIDI) — rebuilt with `make`
build/

# Zip backups / archives of edited sources
*.zip

# Preview output (local + CI); auto-cleaned, but ignore if interrupted
.preview-out/
.ci-preview-out/
.preview-work-*/
